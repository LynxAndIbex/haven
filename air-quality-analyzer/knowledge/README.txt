Place any EPA law PDFs or research paper PDFs into this folder.
When you restart the backend server (`node server.js`), it will automatically parse, chunk, and embed them using Langchain and `nomic-embed-text` so the AI knows about them.
